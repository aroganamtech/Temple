import React from 'react';
import { Box, Typography } from '@mui/material';

const UserVerify = () => {
    return (
        <Box sx={{ p: 4 }}>
            <Typography variant="h4">User Verification</Typography>
        </Box>
    );
};

export default UserVerify;
